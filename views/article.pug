extends layout

block content
	h1= article.title
	h6 Creted by #{author}
	p= article.body
	if user
		if user.id === article.author
			a.btn.btn-primary(href="/articles/edit/" + article._id) Edit
			a.btn.btn-danger.delete-article.ml-2(href="#", data-id=article._id) Remove